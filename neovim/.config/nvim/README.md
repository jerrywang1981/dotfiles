# nvim

configuration for neovim with node required



# jdtls
in folder 'f'
run
```
mkdir jdtls
tar xzvf jdt-language-server-latest.tar.gz -C jdtls
```
and make sure `workspace` folder in your home directory
